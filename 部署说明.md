# 个税社保公积金计算器 — 部署说明

## 仓库配置

项目配置了两个远程仓库：

| 远程名 | 地址 | 用途 |
|--------|------|------|
| `origin` | `git@gitee.com:xbscs/tax-calculator.git` | Gitee（码云） |
| `github` | `https://github.com/a181553515-web/tax-calculator.git` | GitHub Pages 部署 |

当前分支 `master` 追踪 `github/master`。

## 部署原理

- **没有 GitHub Actions**，部署完全依赖 GitHub Pages 原生机制
- GitHub 仓库设置 Pages 直接从 `master` 分支根目录托管
- 根目录下的 `CNAME` 文件（内容 `xiaobo-tax.cn`）绑定自定义域名
- 推送代码到 GitHub 后 Pages 自动更新，一般几秒到一两分钟生效

## 日常维护推送流程

```bash
cd /Users/zhangjingbo/Desktop/个税社保公积金计算器项目

# 1. 查看改动
git status

# 2. 暂存
git add .

# 3. 提交
git commit -m "描述改动"

# 4. 推送到 GitHub（触发 Pages 更新）
git push github master

# 5. 同步到 Gitee（可选）
git push origin master
```

## 政策数据更新的审核流程

社保公积金数据属于高风险、易变更信息，不建议直接在 `master` 修改后推送。推荐流程：

```bash
cd /Users/zhangjingbo/Desktop/个税社保公积金计算器项目

# 1. 在审核分支完成数据和页面改动
git switch -c codex/城市名-社保政策更新

# 2. 运行本地测试与预览
node --test tests/*.test.js
python3 -m http.server 4173 --bind 127.0.0.1

# 3. 逐项复核：统筹区、适用对象、有效期、所有险种、公积金、官方来源
# 4. 只暂存本次改动文件，再提交审核分支
git add data/social-insurance/ js/ social-insurance.html tests/ CHANGELOG.md
git commit -m "feat: 更新某市社保政策"

# 5. 审核确认后才合并到 master，并再次测试后推送 GitHub Pages
git switch master
git merge --no-ff codex/城市名-社保政策更新
node --test tests/*.test.js
git push github master
```

每个可自动计算的地区都必须在 `data/social-insurance/README.md` 的规则下标记为 `verified`；资料不完整时保留 `reference`，不要为了覆盖面输出估算金额。

## 访问地址

| 页面 | 地址 |
|------|------|
| 个税计算器 | `https://xiaobo-tax.cn/` |
| 社保公积金 | `https://xiaobo-tax.cn/social-insurance.html` |

默认 GitHub 域名：`https://a181553515-web.github.io/tax-calculator/`

> 两个页面在同一个仓库、同一个 `master` 分支，推送一次就同时更新。
