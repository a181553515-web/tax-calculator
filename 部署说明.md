# 个税社保公积金计算器 — 部署说明

## 仓库配置

项目配置了两个远程仓库：

| 远程名 | 地址 | 用途 |
|--------|------|------|
| `origin` | `git@gitee.com:xbscs/tax-calculator.git` | Gitee（码云） |
| `github` | `https://github.com/a181553515-web/tax-calculator.git` | GitHub Pages 部署 |

当前分支 `master` 追踪 `github/master`。

## 部署原理

- **没有 GitHub Actions**，部署完全依赖 GitHub Pages 原生机制
- GitHub 仓库设置 Pages 直接从 `master` 分支根目录托管
- 根目录下的 `CNAME` 文件（内容 `xiaobo-tax.cn`）绑定自定义域名
- 推送代码到 GitHub 后 Pages 自动更新，一般几秒到一两分钟生效

## 日常维护推送流程

```bash
cd /Users/zhangjingbo/Desktop/个税社保公积金计算器项目

# 1. 查看改动
git status

# 2. 暂存
git add .

# 3. 提交
git commit -m "描述改动"

# 4. 推送到 GitHub（触发 Pages 更新）
git push github master

# 5. 同步到 Gitee（可选）
git push origin master
```

## 访问地址

| 页面 | 地址 |
|------|------|
| 个税计算器 | `https://xiaobo-tax.cn/` |
| 社保公积金 | `https://xiaobo-tax.cn/social-insurance.html` |

默认 GitHub 域名：`https://a181553515-web.github.io/tax-calculator/`

> 两个页面在同一个仓库、同一个 `master` 分支，推送一次就同时更新。
